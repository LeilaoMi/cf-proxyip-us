# cf-proxyip-us 续接记录（2026-06-04）

## 任务目标
根据 `docs/audit-2026-06-04.md` 制定阶段推进计划，并陆续落地改进。

## 项目路径
`/home/workspace/Projects/cf-proxyip-us`

## 当前分支/提交
- 分支：`main`
- 起始提交：`eacd819`
- 注意：任务开始时已有未提交改动：`README.md` 修改、`docs/audit-2026-06-04.md` 未跟踪。不要覆盖这些既有改动。

## 已完成
- 读取 `/home/workspace/CLAUDE.md`，确认四原则：先想后写、简单优先、外科手术式改动、验证闭环。
- 读取 `Skills/cf-proxyip-deployer/SKILL.md`，确认该项目应保持“本地/CI 检测 + Worker 只分发”，不在 Worker 内批量扫描。
- 检查关键文件：`README.md`、`.github/workflows/proxyip-auto-update.yml`、`worker.js`、`scripts/auto_update.py`、`scripts/audit.py`、`scripts/sync_kv.py`。

## 关键决策
- 先做本地代码与 CI 安全改动，不执行线上部署、DNS 修改、Worker 发布。
- 第一阶段优先处理：legacy 日期 token、`/token` 缓存、Bearer token、workflow 前置验证、独立 `validate_outputs.py`、`auto_update.py` git push 安全化。

## 下一步准确操作
1. 新增阶段计划文档：`docs/implementation-plan-2026-06-04.md`。
2. 新增纯本地验证脚本：`scripts/validate_outputs.py`。
3. 修改 `scripts/auto_update.py`：调用独立验证脚本；live 验证用 `Authorization: Bearer`；commit 只 add 白名单；push 前 rebase。
4. 修改 `worker.js`：
   - `PROXYIP_SECRET` 缺失时不再接受日期 token；
   - `/token` 使用 `private, no-store`；
   - 支持 `Authorization: Bearer <token>`；
   - Bearer token 合法时跳过 UA 黑名单。
5. 修改 workflow：checkout `fetch-depth: 0`；增加 `python3 -m py_compile`、`node --check`、`python3 build_dataset.py`、`python3 scripts/validate_outputs.py` 前置步骤，再运行 auto_update。
6. 更新 README 对认证与 CI 说明。
7. 运行验证：`python3 -m py_compile build_dataset.py scripts/*.py`、`node --check worker.js`、`python3 scripts/validate_outputs.py`、必要时 dry-run 检查 git diff。

## 不可执行/注意事项
- 不要输出或读取任何 secret/token 值。
- 不要执行 `wrangler deploy`、DNS 同步、KV 同步、GitHub push，除非用户明确确认线上操作。
- 不要移动旧副本，除非用户确认；可先在文档中标记 canonical path。

## 当前状态
准备实施第一阶段本地改动。

## 2026-06-04 继续推进记录（二）

### 本轮已完成
- `build_dataset.py`
  - `direct_https` fallback 标记为 `fallback_unverified`，不再给真实高分等级。
  - fallback 排序增加额外惩罚，避免和 cmliu 主检测结果同权竞争。
  - Top5 使用更严格 ASN 分散：同 ASN 最多 1 个。
  - standby 使用更严格 ASN 分散：同 ASN 最多 2 个。
  - 输出 `kv-manifest.json` 时加入 `all_txt/us_txt/best_txt/top5_txt/base64_txt/v2ray_txt` 轻量 key。
- `worker.js`
  - 增加 result_json isolate 内存缓存（60 秒）。
  - `/current.txt`、`/all.txt`、`/us.txt`、`/best.txt`、`/top5.txt`、`/base64.txt`、`/v2ray.txt` 优先读取 KV 轻量文本 key。
  - `/current.txt` 缓存降为 60 秒。
  - ETag 从仅 `checked_at` 改为包含 current/count 或文本内容 hash。
- `README.md`
  - 补充 `allowlist.txt` / `denylist.txt` 格式示例。

### 本轮关键验证
- 已运行：`node --check worker.js`
- 已运行：`python3 -m py_compile build_dataset.py scripts/*.py`
- 已用小脚本验证 fallback 排序惩罚与 Top5 ASN 分散逻辑。

### 尚待最终验证
- 运行 `python3 scripts/validate_outputs.py`
- 运行 `git diff --check`
- 检查 `git diff --stat`

### 注意事项
- 仍未执行部署、DNS/KV 同步、GitHub push。
- 旧副本 `/home/workspace/cf-proxyip-us` 仍存在，未移动，避免未经确认删除用户文件。

## 2026-06-04 继续推进记录（三）

### 用户指令
用户要求“全部推进，要部署前告诉我”。本轮只做本地改动与验证，未执行部署、DNS/KV 同步或 GitHub push。

### 本轮已完成
- 新增 `tests/`：
  - `tests/test_build_dataset.py`：覆盖 fallback 降权、Top5 ASN 分散、standby ASN 分散。
  - `tests/test_validate_outputs.py`：覆盖当前 docs 输出一致性验证。
  - `tests/test_worker_static.py`：覆盖 Worker Bearer token、安全缓存、CORS 范围、`/token` no-store 等静态安全约束。
- `.github/workflows/proxyip-auto-update.yml`：生成/验证后加入 `python3 -m unittest discover -s tests`。
- `build_dataset.py`：新增 `docs/ip_history.json` 生成逻辑，记录近 7 天每个 IP 的 success/fail 次数、最近成功时间、平均延迟、最近 stable score。
- `docs/kv-manifest.json`：加入轻量文本 KV key，并更新当前快照。
- `README.md`：统一为简体中文，补充客户端把 `proxyip.leilaomi.cc.cd` 填到 ProxyIP 字段的说明。
- 旧副本 `/home/workspace/cf-proxyip-us` 已移入 `/home/workspace/Trash/cf-proxyip-us-old-copy-20260604-203322`，并更新 `/home/.z/trash.json` 元数据。
- 本地重新运行 `build_dataset.py`，当前本地快照有效数从 146 变为 140，current 仍为 `43.170.25.96`。

### 验证结果
通过：
```bash
python3 -m py_compile build_dataset.py scripts/*.py
node --check worker.js
python3 scripts/validate_outputs.py
python3 -m unittest discover -s tests
git diff --check
```
`validate_outputs.py` 输出：
```json
{"ok": true, "current": "43.170.25.96", "valid_count": 140, "target_countries": ["US"]}
```
单测输出：`Ran 6 tests ... OK`。

### 部署前停止点
下一步如果继续，就是线上操作：
1. GitHub commit/push；
2. GitHub Actions 或本地执行 `scripts/auto_update.py`；
3. 同步 KV；
4. 同步 DNS-only A 记录；
5. 部署 Worker；
6. 验证线上 `/health`、Bearer、403、CORS、`proxyip.leilaomi.cc.cd` DNS。

必须等待用户确认后再执行。

## 2026-06-04 部署完成记录

### 提交与推送
- `92e864b Harden ProxyIP automation before deployment`
- `d132b25 Refresh ProxyIP data after stricter ranking`
- 已 push 到 `origin/main`，本地 `main` 与 `origin/main` 对齐。

### 部署结果
- 执行命令：`PROXYIP_SKIP_GENERATE=1 python3 scripts/auto_update.py`
- KV 同步成功，包含轻量 key：`current_txt`、`standby_txt`、`top5_txt`、`all_txt`、`us_txt`、`best_txt`、`base64_txt`、`v2ray_txt`、`ip_history_json` 等。
- DNS 同步结果：`changed=false`，`proxyip.leilaomi.cc.cd` 仍指向 `43.170.25.96`。
- Worker 部署成功：Version ID `3ed7963c-c2c0-4836-8d49-11dd3f064897`。
- live audit 通过：`ok=true`，`token=hmac`，`health_count=140`，`standby_count=10`。

### 线上验证
- `/health`：200，`count=140`，`data_source=kv`。
- `/current.txt` 未认证：403，符合设计。
- `/token`：200，`cache-control: private, no-store`，返回 HMAC token。
- `Authorization: Bearer <token>` 访问 `/current.txt`：200，返回 `43.170.25.96`，`cache-control: private, max-age=60`，无 `access-control-allow-origin`。
- `Authorization: Bearer <token>` 访问 `/health/full`：200，`ok=true`，`current=43.170.25.96`。

### 最终验证命令
```bash
python3 -m py_compile build_dataset.py scripts/*.py
node --check worker.js
python3 scripts/validate_outputs.py
python3 -m unittest discover -s tests
git diff --check
```
结果：全部通过。
